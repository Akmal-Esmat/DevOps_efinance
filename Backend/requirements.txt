# Trivy did not detect.
# fastapi[standard]
# uvicorn[standard]
# openai
# python-dotenv >= 1.2.2

# Trivy detected it.
# fastapi[standard]
# uvicorn[standard]
# openai
# python-dotenv == 1.2.2

# Trivy did detect (all versions are exact =) and broke (broken versions).
# fastapi[standard]==0.65.0
# uvicorn[standard]==0.13.4
# openai==0.27.0
# python-dotenv==0.15.0

# Trivy did not detect (all versions are exact >) and did not break (broken versions!).
# fastapi[standard]>=0.65.0
# uvicorn[standard]>=0.13.4
# openai>=0.27.0
# python-dotenv>=0.15.0

# Working.....
fastapi[standard]==0.138.1
uvicorn[standard]==0.49.0
openai==2.44.0
python-dotenv==1.2.2